#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

long summation(vector<int> &values)
{
    return 0;
}

int main() {
    long n;
    vector<long> a;
    long tmpA;
    cin >> n;
    
    for(int i = 0; i < n; i++)
    {
        cin >> tmpA;
        a.push_back(tmpA);
    }
    
    cout << summation(a);
    
    return 0;
}
